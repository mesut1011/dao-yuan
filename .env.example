# Optional server-side API integration.
# The app works without these values by returning a local starter director plan.
AI_DIRECTOR_API_URL=
AI_DIRECTOR_API_KEY=

# Optional customer registration API integration.
# Without these values, POST /api/customers/register uses the in-memory starter store.
CUSTOMER_REGISTRATION_API_URL=
CUSTOMER_REGISTRATION_API_KEY=
